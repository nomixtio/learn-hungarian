# alchemy@2.0.0-beta.77 declares a peerOptional vite@^8 while the app uses
# vite@6. Strict peer resolution (npm ci) fails on this, so relax it
# repo-wide. Verified: build + unit/worker tests green with this setting.
legacy-peer-deps=true
